# Requirements: Date Helper Function

## Goal

Create a utility function `formatDate(date: Date, format: string): string`.

## Features

- Support "YYYY-MM-DD" format.
- Support "DD/MM/YYYY" format.
- Handle invalid dates gracefully (throw error or return empty string).
- Written in TypeScript.
- Should export the function.
